apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-request-handler-mcp
  labels:
    app: group-request-handler-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: group-request-handler-mcp
  template:
    metadata:
      labels:
        app: group-request-handler-mcp
    spec:
      containers:
        - name: group-request-handler-mcp
          image: group-request-handler-mcp:local # 本地或仓库中的镜像名
          imagePullPolicy: Never  # 使用 kind 或本地 k8s 时
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
          readinessProbe:
            httpGet:
              path: /ping
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
