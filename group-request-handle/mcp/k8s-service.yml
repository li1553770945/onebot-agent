apiVersion: v1
kind: Service
metadata:
  name: group-request-handler-mcp
  labels:
    app: group-request-handler-mcp
spec:
  type: NodePort
  selector:
    app: group-request-handler-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 31300
