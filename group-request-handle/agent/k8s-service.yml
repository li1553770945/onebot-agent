apiVersion: v1
kind: Service
metadata:
  name: group-request-handler-agent
  labels:
    app: group-request-handler-agent
spec:
  type: ClusterIP
  selector:
    app: group-request-handler-agent
  ports:
    - name: http
      port: 3000
      targetPort: 3000
